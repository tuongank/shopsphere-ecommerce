name: CI

on:
  pull_request:
    branches: [ develop, main ]
  push:
    branches: [ develop, main ]

jobs:
  placeholder:
    name: Placeholder CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate repository structure
        run: |
          test -f README.md
          test -f docs/git-workflow.md
          test -f .github/pull_request_template.md
          echo "Repository structure looks good. Replace this job after selecting the backend stack."
